{
  "name": "ecportal",
  "version": "0.1.0",
  "private": true,
  "description": "ecportal — 支部/旅系統與公開頁 (BUILD.md 為唯一真理)。Vercel 靜態部署，體積治理優先。",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "lint": "node scripts/check.mjs --lint",
    "build": "node scripts/build.mjs",
    "slim": "node scripts/check.mjs --slim",
    "slim:dead": "node scripts/check-dead-assets.mjs",
    "slim:size": "node scripts/check-size.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
