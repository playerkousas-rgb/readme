---
version: v4.9.0
date: 2026-09-22
status: FINAL
---

# 框架建構原則 — 由 ecportal 實戰提煉

定位: 本 spec (01-11) = **平台框架**，所有支部共用；每個支部只喺 12.7 清單內**微調**，唔 fork 核心代碼。
ecportal (Code.gs v2.6.3 + api/*) 就係呢套框架嘅參考實現兼活教材。

## 12.1 分層 — 框架 vs 支部微調
- 框架提供不變核心: 帳號/信任鏈、同步（樂觀鎖+merge3）、備份、審計、模組註冊、權限封頂
- 支部微調只限 12.7 清單；核心代碼一律單一來源
- 理由: 實戰證明一套代碼多支部共用先修得完 BUG；每支部一套 = BUG 翻倍冇人修

## 12.2 「fix 唔到嘅 BUG」嘅病根 → 框架規矩
ecportal 史上最難纏嘅 BUG（82 vs 0082 對唔上、寫咗但讀唔到、staging 垃圾行、分頁分身物資·0082、兩套欄序讀錯欄）病根全部一樣: **兩套真理**。框架規矩:
1. 每種數據**一個正典 key**；報表/分頁永遠由正本派生，唔可以第二個寫入路（v2.6.3「一次儲存兩處都寫」已行對路，03 固化）
2. ID 正規化只有一個 normId（02）；單位校正由 server 做統一（proxy canonUnit 已示範）
3. 版本永遠 **server 派**（ISO+隨機尾數，唔信 client 時鐘）— 08 B1 定案
4. action 白名單用 **lint 自動比對**前後端（scripts/lint.mjs 模式 → 框架標配；uploadPhotos 漏白名單事故嘅根治）
5. 刪行/改行一律一個方法、由底往上、一次過（deleteRowRuns 教訓 → 03 原子寫）

## 12.3 「制同功能亂飛亂指」→ UI 模組註冊制
- 每個功能 = 一個模組，註冊時聲明: 名、入口位置、所需權限、開關（接 10.5）
- 導航由註冊表**自動生成**，唔准散裝 hardcode 掣；最多兩層，新人 day-1 搵到嘢
- 支部微調淨可以: 改名、排序、開關 — 唔可以自己發明第四層目錄或者藏掣

## 12.4 同步出錯 → 標準同步診斷
- dbInfo 模式標準化: `{version, bytes, sizes, stagingRows, conflict 計數, backendVersion}` →「同步診斷」一頁睇晒
- 三色燈（05 黃/綠/紅）+ 診斷報告一鍵複製（領袖求援貼去群組就有人識答）
- 後端版本握手: `status` 回 `backendVersion`，前端偵測舊後端就提示「貼新 Code.gs + 部署新版本」（ecportal BACKEND_VERSION 已示範）
- 分段讀寫每段核 version，變咗由頭再讀（防半新半舊）— 已入 03

## 12.5 JSON 備份 = 一等公民（09.5 強化）
- **離線都匯得**: 本地 localStorage 有幾多匯幾多，唔靠後端 — ecportal exportAll 已示範，用戶實測「真係備份到」
- 匯出檔格式: `{meta:{unitCode, exportedAt, version, sha256}, data}` — 跨旅/跨版本可讀，還原 importAll 走 03 原子寫
- 三時機自動提醒備份: 版本升級前 / 批量操作前 / 超過 7 日冇備份
- 每季還原演練（09.5）；備份檔本身含 hash（X2 教訓: 唔好將成員 hash 備份落唔安全地方，匯出可選「剝密碼欄」）

## 12.6 QR 公開資料 = 標準輸出（10.2 延伸）
- 每個公開資料項（通告/團章/物資借用/報名/相簿）自動生成: 公開短連結 + **QR code**（列印用，用戶實測好用）
- **QR 只帶公開 URL**（例 `/notice.html?u=0082`），**永不帶 exec URL 或 API key** — 帶 key 嘅 QR 一印出街＝貼 key 喺壁報板（X1/X6 教訓）
- 公開頁只讀 `published`/公開 scope（Code.gs loadPublicNotices 模式 → 標準）
- 匿名可寫頁（報名/借用/申報）QR 旁邊要印「有效期＋關閉方法」— 領袖要識得收線（對應 X8/X9 限流）

## 12.7 支部微調清單（框架留俾支部嘅全部）
可以掂: 模組開關（10.5）、公開類別定義（10.2）、行事曆標籤（10.6）、訂閱接收、欄位微調、文案/主題、QR 樣式
唔可以掂: auth/信任鏈、同步核心、備份格式、審計、ID 規則、權限封頂
