# 進度追蹤旅系統升級版 — 跟住改

> 舊進度已完成，現升級旅系統。照做就得。

## 變數 ABCD（唔入 SHEET）

- A = `SUPER_KEY`（超管隱藏，與旅無關）
- B = `TROOP_(id)_BACKEND`（部署後抄 URL）
- C = `TROOP_(id)_NAME`（交 ADMIN 時填，自己旅名自己知）
- D = `TROOP_(id)_APIKEY`（生成後交 ADMIN 轉登記）

B/D 由 GS 生成，C 自己填，A 隱藏。**四個唔寫入任何 SHEET**，經收件匣交 ADMIN 登記。

## 要做

1. **部署 GS**，抄 B 的 URL，生成 D
2. **填 C**，連 A/B/D 經收件匣交 ADMIN
3. **上游控下游寫**：掣在上游，旗在下游 `ScriptProperties`，閂後只收 `sig`
4. **開戶**：閂口後上游揀團開戶

## 唔做

- 不在 SHEET 寫 ABCD
- 不改 A
- 不設回調

## 清理

- GS 內 `// vX.X` 註解全拆，版號只留本 MD
