# 進度追蹤旅系統升級版

> 已完成進度追蹤，現升級旅系統用。本文件為唯一升級說明，`BUILD.md` 為準。

## 功能變數

- **A**：支部 SHEET（1團1張）
- **B**：進度 SHEET（1團1張，B模式：支部「進度」頁經後端讀 B）
- **C**：旅 SHEET（`TROOP_OPS` 每團一行）
- **D**：超管隱藏（`SHEEP + EC_SUPER_KEY`，僅 SUPER，不在 SHEET/前端）

`A/B/C` 互為上下游，`D` 獨立隱藏。

## 預設接入（上游）

- 未掛接前：各自獨立登入
- 掛接後：**上游控、下游寫** — 開關掣在上游，旗寫在下游 `ScriptProperties`，下游閂口後只收 `sig`
  - 團掛進度：支部控進度
  - 旅掛團：旅控團
- 新戶：閂口後由**上游揀團開戶**，經 `sig` 落下游寫

## 不走 JSON 登記

- 不再用 JSON 匯出/匯入登記
- 接入走 `TROOP_OPS` 追加行，不改 `Vercel env`

## 禁止

- 禁改 `SHEEP` → `SHEET`
- 禁 SHEET 寫 `API KEY / BACKEND URL`
- 禁回調

*詳見 `BUILD.md §1 §2`*
