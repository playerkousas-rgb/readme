# 進度追蹤旅系統升級版 — UPDATE 懶人包

> **俾已完成進度追蹤嘅同事** — 唔使由頭睇 BUILD.md，淨係睇呢份做 UPDATE  
> 完整真理仍以 `BUILD.md` 為準，呢份只係**變更點 + 點改 + 點測**  
> ⚠️ 第1行「不準事項」就係因為上次有人將進度追蹤當通告咁加圖書館邏輯搞出事，今次**只做下面3件事**

---

## 0. 一句話講晒要改咩

1. **1團1張**：支部、進度各 1團1張 SHEET（5團=10張）+ 旅1張 `TROOP_OPS` 每團一行存兩把 key
2. **B模式**：支部前端個「進度」頁唔再讀自己 SHEET，改為經 `TROOP_OPS` 同行 `PROGRESS_BACKEND/APIKEY` 打進度 `/exec`
3. **上游控、下游寫**：下游本地入口 `ALLOW_LOCAL_LOGIN` 旗寫喺**下游 GS**，掣只喺**上游前端**，閂口後只收 `sig`

其餘通告/行事曆/相簿唔郁。

---

## 1. 你而家嗰份進度要點改（3個位）

### A. 後端 `Code.gs`
- 新增 `setDownstreamAccess({allowLocal})`：只接受**上游 `sig` 驗證**先可寫 `ScriptProperties ALLOW_LOCAL_LOGIN`
- 本地 `doPost` 開頭加 `if (!ALLOW_LOCAL_LOGIN && !isSig) return 403`（登入/開戶都攔，`upsertUser/setPw` 等 server-to-server 放行）
- 預設新部署 `ALLOW_LOCAL_LOGIN=true`，單用時唔會誤閂

### B. 前端
- **進度前端**：唔使加「關入口」掣
- **支部前端**：`「進度」頁` 改為 `fetch(PROGRESS_BACKEND)`（B模式），並加「關閉進度直接登入」掣（只支部領袖見，調上面個 `setDownstreamAccess`）
- **旅前端**：加「揀團開戶」選單（讀 `TROOP_OPS` 行，經 `sig` 落該團支部寫，AUDIT `via=sig`）+ 「關閉團直接登入」掣（只旅長見）

### C. Registry
- `TROOP_OPS` 改為**每團一行**：`branchId | BRANCH_BACKEND | BRANCH_APIKEY | PROGRESS_BACKEND | PROGRESS_APIKEY | ALLOW_TROOP_LOGIN`
- **追加唔搬**：旅掛團時只追加行，**唔改 `units.json/Vercel env`**（唔使 ADMIN 郁 ENV）

---

## 2. 已有用緊進度嘅舊數點搬（JSON 吐出）

> 無咗呢步，新支部會係空殼

1. 舊進度 SHEET → 按「匯出成員 JSON（含 hash）」（`exportAll` 預設剝密碼，呢次揀**含 hash+salt**）
2. 新支部 SHEET → 按「匯入批量開戶」→ 後端逐個 `upsertUser` **直插 hash**（唔經 `1234+mustChangePw`），`transferId` 冪等 + `sha256` 驗 + 撞號阻擋
3. 匯完即閂進度本地入口（支部前端掣），之後改密碼走 `setPw` 同步落進度，分批 `verifyPw` 核對

*童軍團少人，5-10個一批夠，唔使擔心 100+*  
*情景：先進度→後團→後旅 用逆向；先團→後進度 用正向（同一個掣，方向相反）*

---

## 3. 開戶同跨團點做（閂口後）

- **新成員/新領袖**：團口已 403，一律**喺上游揀團開**（旅→團、團→進度），寫入仍在下游
- **教練員（跨團）**：旅層 `branch_access`，旅長直開多團
- **本職領袖想兼幫他團**：上游發「幫手申請」→ **目標團領袖批** → `TROOP_OPS` 先追加 `branch_access`（`grantedBy=目標團`），目標團可撤。旅長唔可繞過目標團。

---

## 4. 5情景一覽（你屬於邊個照做）

| 你而家 | 要做 |
|---|---|
| **A. 全新旅** | GS 一次過生 1旅+N×2，匯入後閂下游 |
| **B. 有團+進度，想加旅** | 追加 `TROOP_OPS` 行 + 種首個旅長，不郁 ENV |
| **C. 只有進度，想加團再加旅** | **JSON 逆向倒灌** → 加團 → 加旅 |
| **D. 只有進度，想一次過加團+旅** | 同C一次過 |
| **E. 有團，想加旅再加進度** | 加旅後用 JSON 正向同步團→進度 |

---

## 5. 自測 Checklist（交貨前必過）

- [ ] 單用進度時無「關入口」掣，唔會誤閂
- [ ] 團掛進度後，進度本地登入 403，只接受旅/團 `sig`
- [ ] 旅掛團後，團本地登入可被旅一鍵閂，旅揀團開戶 `via=sig` 成功
- [ ] JSON 含 hash 匯入後舊密碼照用，唔使 `1234`
- [ ] 跨團兼幫未經目標團批，加唔到 `branch_access`
- [ ] `TROOP_OPS` 追加行後 `flush` 即生效，ENV 無被改

---

*有疑問睇 `BUILD.md §1 入口開關 / §2 開戶錨點+JSON / §3 備份`，AGENTS.md §6 自檢亦已同步。*
