# 進度追蹤旅系統升級版 — UPDATE 對照表（人看）

> **對象：已完成舊版「進度追蹤」要升級做「旅+團+進度」嘅同事**  
> **性質：本文件只係對照表，方便你唔使成份 BUILD.md 逐行搵。實作唯一真理 = `BUILD.md` + `AGENTS.md`**  
> **AGENT 注意：本文件唔係執行清單，唔好直接改碼**

---

## 🚫 禁止事項（上次 AGENT 睇錯搞壞系統，今次寫死）

1. **禁止改 `SHEEP` → `SHEET`**：`SHEEP` 係 `BUILD.md §2` 嘅 SUPER 專用（`sheep + EC_SUPER_KEY`），同 Google SHEET 無關，任何人/AGENT 不得將變數名、註解、env 名由 `SHEEP` 改做 `SHEET`
2. **禁止在 SHEET 儲存格寫 `API KEY` / `BACKEND URL`**：`BRANCH_APIKEY / PROGRESS_APIKEY / *_BACKEND` 只存 **Vercel env** 或 **TROOP_OPS 表的 server 端**（server-to-server 讀），**永不入 SHEET 儲存格、永不入前端、永不入 URL/QR**（`BUILD.md §1`）
3. **禁止設回調（callback / onEdit / trigger）**去同步支部↔進度 或 旅↔團：同步只走 `sig` + `upsertUser/setPw/setStatus/verifyPv` server-to-server 鏈（`BUILD.md §2`），唔准在 SHEET 加公式/腳本回調
4. **禁止將下游 `ALLOW_LOCAL_LOGIN` 做成 SHEET 儲存格**：佢係**下游 GS ScriptProperties** 旗，由**上游經 `sig` 寫入**，唔係儲存格

違反以上 = 回滾。

---

## 0. 要升級咩？一句話

- 舊：1個進度 SHEET 獨立跑
- 新：**1團1張支部 SHEET + 1團1張進度 SHEET + 旅1張 TROOP_OPS**（例 5團=11張），支部前端「進度」頁經後端代理讀進度（B模式），上游可一鍵閂下游直接入口

通告/行事曆/相簿唔使改。

---

## 1. 舊進度要升級，哪幾個概念要對齊（唔係貼 code）

- **Registry**：`TROOP_OPS` 由無變有，**每團一行**記該團兩把 key（追加，**不改 `units.json/Vercel env`**，免 ADMIN 動 ENV）
- **入口開關**：閘在**下游**，掣在**上游**。未掛接前大家都開；團掛進度後、旅掛團後，上游領袖/旅長先會見到「關閉直接登入」掣，按落去經 `sig` 寫下游旗，下游之後只收 `sig`
- **開戶位置不變**：成員永遠在**該團支部**誕生，領袖按所屬層（`BUILD.md §2 開戶錨點`）。閂口後新戶改由**上游前端揀團**（選單），實際經 `sig` 落下游寫，唔係寫入上游

---

## 2. 舊數點搬（否則新支部係空）

- 流程：**舊進度「匯出 JSON（含 hash）」→ 新支部「匯入批量開戶」直插 hash**（保留舊密碼，唔經 `1234`），`sha256` 驗 + 撞號阻擋 + 冪等
- 技術位請睇 `BUILD.md §2 JSON 吐出 + §3 備份`，唔好自己在 SHEET 寫公式
- 少人團 5-10個一批已經夠

---

## 3. 開戶 & 跨團（閂口後）

- 新人：閂口後一律**上游揀團開**（旅→團、團→進度）
- **教練員（跨團）**：旅層 `branch_access`，旅長開
- **本職領袖兼幫他團**：要**目標團領袖批**先追加 `branch_access`，旅長唔可繞過

---

## 4. 你屬哪種，照做哪行

| 現況 | 做咩 |
|---|---|
| 全新旅 | 一次過生 1旅+N×2 |
| 有團+進度想加旅 | `TROOP_OPS` 追加行，不郁 ENV |
| 只有進度想加團/旅 | JSON 倒灌（逆向）再掛 |
| 有團想加進度 | JSON 正向同步 |

詳細 5情景見 `BUILD.md §1 新團開通次序`。

---

## 5. 交貨前自測

- [ ] SHEET 儲存格內搜 `APIKEY / BACKEND / sheep` 零結果
- [ ] 無新增 `onEdit / callback / trigger` 去同步
- [ ] 單用進度時無「關入口」掣；掛接後上游掣可閂，下游本地 403 只收 `sig`
- [ ] JSON 匯入後舊密碼照用
- [ ] 跨團兼幫未經目標團批加唔到

*有疑問睇 `BUILD.md`，唔好估。*
