# 進度追蹤旅系統升級版 — 對齊定案 (2026-09-23)

> **舊進度已完成，現升級旅系統。照做就得。**
>
> 本檔 = 本輪升級嘅**規格／版號唯一記錄**（程式碼內唔寫 `// vX.X` 註解，版號只留本 MD，見 §9）。
>
> **對齊對象（as-built，唔係計劃稿）**：`vsbadge` `operations/TROOP_LINK_UPGRADE.md`（PR #19）、`roverbadge` `docs/TROOP_UPGRADE_2026.md`（PR #23／#24）。兩個 repo 嘅碼已經行得通、測試全綠（見 §10），所以本 MD 由「計劃」改寫成「實作規格」，其他支部照本 MD 複製。

---

## 0. 一句話

同一份 `Code.gs` 部署喺**每一層**（旅／團（支部）／進度）。**上游登記下游嘅 URL + SHEET KEY 就可以讀寫下游**（進咗上游就等於進咗下游）；開咗上游之後，**上游前端一鍵閂下游直接入口**（旗 `ALLOW_LOCAL_LOGIN`，寫喺下游 `ScriptProperties`），閂口後上游照收、本地入口回 `403`。

接入完全自願：唔登記下游、或者登記咗但唔閂口，現有旅團一切照舊（旗未設定＝開啟，行為零變化）。

---

## 1. 變數 ABCD（四個都唔寫入 SHEET）

| 代號 | 名稱 | 邊個產生 | 放喺邊 | 用途 |
|---|---|---|---|---|
| **A** | `SUPER_KEY` | 現有（隱藏） | Vercel env | 超管登入。**與旅系統無關，本輪不改動、不在任何選單顯示** |
| **B** | `TROOP_(id)_BACKEND` | GS 部署後抄（`ScriptApp.getService().getUrl()`） | Vercel env；上游則存 `DOWNSTREAM_<id>_URL` | 主系統 proxy／上游打去邊個 GAS |
| **C** | `TROOP_(id)_NAME` | 你自填 | Vercel env | 前端旅團顯示名（純文字） |
| **D** | `TROOP_(id)_APIKEY` | GS 生成（存在 Script Properties `API_KEY`） | Vercel env；上游則存 `DOWNSTREAM_<id>_KEY` | apikey，同時係 `sig` 嘅根密鑰（§3） |

- **一次過抄 B + D**：Sheet 選單「**🔗 旅系統 → 🔑 顯示 BACKEND／APIKEY（交 ADMIN）**」。
- **交 ADMIN**：經收件匣（首頁「📋 新旅團部署」→ `submitRegistration` → `/api/proxy` → 伺服器端固定嘅中央收件端點）。交 **B、C、D**；ADMIN 入 Vercel env 後 Redeploy。
- **四個都唔寫入任何工作表**：B/D 只存 Script Properties（同 Vercel env），C 由你填，A 隱藏。有測試掃晒所有工作表守呢條。
- **換 D（撤銷舊 key）＝三處同步**（漏一處就靜靜打唔通）：① 下游 Script Properties 刪 `API_KEY` → 跑 `showApiKey()` 生成新 key；② 上游重新登記下游；③ ADMIN 改 Vercel env + Redeploy。

---

## 2. 上下游接入

```
        旅 GAS ──── sig ────▶ 團（支部）GAS ──── sig ────▶ 進度 GAS
          │                        │                          │
   登記下游 B + D            登記下游 B + D            ALLOW_LOCAL_LOGIN
   DOWNSTREAM_<id>_URL      DOWNSTREAM_<id>_URL         （直接入口掣）
   DOWNSTREAM_<id>_KEY      DOWNSTREAM_<id>_KEY
```

- **每層都係同一份 `Code.gs`**：一個節點可以同時係上游（對住自己嘅下游）同下游（對住自己嘅上游）。
- **上游 Script Properties**：`DOWNSTREAM_<id>_URL`、`DOWNSTREAM_<id>_KEY`、`DOWNSTREAM_<id>_NAME`、`DOWNSTREAM_<id>_AT`。`<id>` 由你改（例：`progress`、`vs0082`、`branch-a`），只可用英文／數字／底線／連字號，最長 32 字元。旅對住幾個團就登記幾條。
- **下游 Script Properties**：`ALLOW_LOCAL_LOGIN`（未設定＝開啟）。
- **掣在上游**：上游選單「🚪 下游直接入口 → 🔒 閂口（只收 sig）／🔓 開啟」，經 `sig` 打下游 `setLocalLogin`。下游自己都有同一個掣（「🚪 本機直接入口」），只作**未接上游時獨立運作／災難恢復**；一掛接上游，**以上游為準**（見 §8）。

### 接入步驟（每團照做）

| 步驟 | 做乜 | 邊度做 |
|---|---|---|
| 1 | 部署 GS，抄 B 的 URL，生成 D | 每層 Sheet → Apps Script → 部署（執行身分「我」、存取權「任何人」）→ 選單「🔗 旅系統 → 🔑 顯示 BACKEND／APIKEY」 |
| 2 | 填 C，連 B/D 經收件匣交 ADMIN | 首頁「📋 新旅團部署」表單 |
| 3 | 每團補一張支部 SHEET，與現有進度 SHEET 成對 | 新 Sheet → 貼同一份 `Code.gs` → `initializeSheets()` → 部署 → 抄 B/D |
| 4 | 上游登記下游，再測連線 | 上游選單「➕ 登記下游（URL + SHEET KEY）」→「📡 測試下游連線（sig）」 |
| 5 | 搬舊數（§7） | 舊進度「📤 匯出 JSON（含 hash）」→ 新支部「📥 匯入 JSON」 |
| 6 | 核對無誤後閂下游直接入口 | 上游選單「🚪 下游直接入口 → 🔒 閂口」（有確認彈窗，提醒先完成匯入及連線測試） |
| 7 | 之後開戶一律在上游 | 上游選單「👤 為下游開戶（揀團）」 |

- 每層升級：Apps Script → 用新 `Code.gs` **全檔覆蓋** → 部署 → **管理部署 → ✏️ 編輯 → 版本選「新版本」**（`/exec` URL 唔變，Registry 唔使改）。
- **只有全新嘅支部 SHEET 先執行 `initializeSheets()`**；現有進度 SHEET 唔好重跑（唔改 schema、唔清資料）。
- 登記下游時 **URL 必須係正式 GAS `/exec`**（`https://script.google.com/macros/s/…/exec`），其他一律拒。

---

## 3. `sig` 點計（GAS → GAS，唔經 Vercel proxy）

```
根密鑰    = 該節點的 SHEET KEY（D）
sigKey    = hex( HMAC-SHA256( message = "<purpose>", key = D ) )
canonical = action + "\n" + ts(毫秒) + "\n" + nonce + "\n" + hex(SHA-256(rawBody))
sig       = hex( HMAC-SHA256( canonical, sigKey ) )
```

- **上游簽出站用「下游的 D」**（登記咗嗰條）；**下游驗入站用「自己的 D」**。密鑰以用途字串分隔推導，推導結果唔落地、唔儲存。
- **兩組 sig 一齊送**（GAS 302 轉址會遺失 query）：query `?sig=&sts=&snonce=`（digest 綁完整原始 body）＋ body `{…, sig, sig_ts, sig_nonce}`（digest 綁去掉三個 sig 欄位後嘅 body）。下游先驗 query，驗唔到再驗 body；**兩組 nonce 一次過消耗**，堵死混合重放。
- **防護**：時窗 ±5 分鐘（未來時間戳都拒）／nonce 一次性（`CacheService` 10 分鐘）／body ≤ 900 KB／`sig` 必須 64 位 hex／比較用「兩邊先各自 SHA-256 再比對」（避免逐字元短路）／**一律 POST，唔收 GET 帶 sig**。
- **`<purpose>` 現時每個 repo 唔同**（`vsbadge-troop-sig-v1`、`roverbadge-troop-sig-v1`），所以**跨 repo 接唔到**。複製去其他支部時必須統一（待決，見 §12）。

### 3.1 action 白名單

- **讀**：`load`、`getLoginMode`、`getLinkState`、`getMembers`、`getConfig`、`getAllUsers`、`getOtherBadges`、`getPendingRequests`、`getApplications`、`getLogRecords`、`getLogRequests`、`getAuditLog`（roverbadge 以 `getPendingLogRequests` 代替 `getLogRequests`、暫無 `getAuditLog`）
- **寫**：`save`、`saveOtherBadge`、`requestComplete`、`reviewRequest`、`addMember`、`addUser`、`bulkAddUsers`（roverbadge 冇）、`upsertUser`、`importUsers`、`resetPassword`、`updateUserProfile`、`setUserStatus`（roverbadge 用 `deactivateUser`／`reactivateUser`）、`deleteUser`、`updateUserRole`、`updatePermissions`、`saveLogRecord`、`deleteLogRecord`、`reviewLogRequest`、`setLocalLogin`
- **永不接受（即使有 sig）**：`login`、`apply`、`logout`、`changePassword`、`updateConfig`、`requestLogRecord`、`cancelLogRequest`，以及白名單以外任何 action。
- 每筆簽名**寫入**都會喺下游「操作紀錄」留一行（操作者 `upstream`，詳情含 `on_behalf`）；讀取唔留。上游傳嚟嘅標籤會先消毒（只留 `0-9A-Za-z_.@-`），防儲存格算式注入。

### 3.2 上游讀得到下游幾多

- 讀 action 令**上游可以直接讀下游**（成員、用戶清單、日誌…）：**回應永不含 `password_hash`**（有測試守）。
- **hash 讀唔到**，所以搬舊密碼仍然只可以行 §7 嘅「JSON 吐出」（下游主動 export，唔係上游拉）。

---

## 4. 回傳／回調（★ 本輪對齊重點）

「回調」同「回傳」係兩件事，寫死三條：

1. **同步回傳 = 有，亦係必要**：上游打下游，下游**即刻回一個結果**（`{success, data}` 或 `{success:false, error}`）。`📡 測試下游連線（sig）` 就係睇呢個回傳；開戶、匯入、閂口亦全部靠回傳知道成唔成功。（失敗要明講，例：「上游已開戶，但下游寫入失敗」，唔可以靜靜地兩邊唔一致。）
2. **非同步回調 = 冇，永遠冇**：旅系統**冇 callback endpoint**，下游**永遠唔會主動回打上游**；sig 流向只有「上游 → 下游」一條。下游亦冇任何定時器／`onEdit` 觸發器（全倉掃 `callback|postMessage|newTrigger|onEdit` 零命中）。
3. **唯一例外係中央登入鏈，唔關旅系統事**：`GAS 回打固定受信端點 /api/super 驗票`（60 秒 AES-GCM 票據、綁定旅團、一次性防重放）——**vsbadge／roverbadge 行呢條，已上線成功**。對照：**cubsbadge／scoutbadge 行「零回打」版**（Vercel 側比對 `SUPER_KEY` 之後送 `action=superLogin`，GAS 完全冇任何回打）。兩套都過得，但**同一生態圈唔應該長期有兩套**（見 §12 待決 ⑤）。

> 一句記法：**上游問一句，下游答一句（回傳）；下游唔識自己開口（冇回調）**。登入鏈例外另計。

---

## 5. 直接入口掣 `ALLOW_LOCAL_LOGIN`（寫喺下游，fail closed）

| 掣值 | 行為 |
|---|---|
| **未設定**（預設） | **開啟** — 現有旅團零影響；`initializeSheets()` 唔會自動寫值 |
| `1`／`true`／`yes`／`on`／`open`（大小寫／空白不敏感） | 開啟 |
| **其餘任何值**（`false`／`0`／`no`／`off` 或串錯字） | **閂口（fail closed）** |

### 閂口後行為對照

| 請求 | 未設定／`true` | `=false`（閂口） |
|---|---|---|
| 前端直接 `login`／`apply` | 照舊 | **拒**，回 `{success:false, upstream_only:true, error:"…只接受上游簽名（sig）請求…"}` |
| `GET ?action=load`／`getLoginMode` | 照舊 | **拒**（同一訊息） |
| 舊 Portal／apikey 直接 `save` | 照舊（兼容） | **拒**（apikey 唔再等於授權） |
| 用戶 token 操作 | 照舊 | **拒** |
| 上游 `sig` 請求 | **接受** | **接受** |
| 中央登入（A） | **接受** | **接受**（與旅系統閘門脫鉤） |

- 閂口係可逆：下游選單「🔓 開啟」，或上游以 `sig` 打 `setLocalLogin(allow=true)`。
- **每個下游至少留一個本地領袖戶 + SUPER 作災難恢復**。

---

## 6. 開戶（閂口後）

**閂口後新戶在上游揀團開戶，經 `sig` 落下游寫。**

- 上游選單「**👤 為下游開戶（揀團）**」（程式介面 `createAccountForDownstream(id, rawUser, manager)`）：① 上游本地開戶（角色權限、YMIS／Email 唯一性照舊）→ ② 讀回 `password_hash` → ③ `sig` 打下游 `upsertUser`。
- 兩邊**同一個 hash**，所以同一個臨時密碼兩邊都啱用；首登仍然強制改密碼。
- `upsertUser` 語義：新戶**必須帶** 64 位 hex `password_hash`；**帶明文 `password` 一律拒**；同 YMIS（或同 Email 認回同一身份）→ **更新**；冇帶 hash → **保留原密碼**；冇帶 `branch` 唔洗走既有小隊；`SUPER_ADMIN_ID` 保留帳號絕不可操作；**冪等**。
- **身份錨點照 BUILD.md §2**：成員喺**該團支部**誕生（下游唔會自己開成員戶口、唔會反寫上游），進度只係身份消費者。

---

## 7. 吐 JSON（搬舊數）

**場景**：舊進度有數，新支部空。

1. **舊進度按「📤 匯出 JSON（含 hash）」**：寫成 Drive 檔 `vsbadge-users-<yyyyMMdd-HHmmss>.json`（建立後即設 `Access.PRIVATE` + `Permission.NONE`）；Drive 失敗時完整 JSON 寫入「檢視 → 執行紀錄（Logger）」作後備；**只寫 Drive／Logger，絕不寫入任何工作表**；「操作紀錄」只記筆數同檔案 ID。
2. **新支部按「📥 匯入 JSON（upsertUser 直插 hash）」**：貼 Drive 連結或檔案 ID，逐個 `upsertUser` 直插 hash（**保留舊密碼**）。只收 64 位 hex hash；明文密碼／假 hash／壞 JSON／缺 YMIS 一律拒；`force_change_password` 跟隨匯出值；一次最多 2000 筆；完成彈窗顯示「新增 X、更新 Y、失敗 Z」。
3. **匯完可閂下游直接入口**（核對筆數 → 上游「🔒 閂口」）。
4. **匯入後即刪 Drive 匯出檔**（含 hash）；唔好用共享資料夾或電郵明文傳送。

---

## 8. 唔做

- ❌ 不在 SHEET 寫 ABCD（有測試守）
- ❌ 不改 A（`SUPER_KEY` 相關邏輯、`api/` 超管路徑全部原封不動）
- ❌ **旅系統不設回調**（§4 第 2 條；同步回傳唔算回調）
- ❌ 不改前端 `index.html`、不改 `api/`（`sig` 係 GAS→GAS，唔經 Vercel proxy，所以 proxy 白名單唔使加）
- ❌ 不改現有工作表 schema、不清資料、現有部署唔使重跑 `initializeSheets()`
- ❌ 進度追蹤**唔加通告／圖書館／推送邏輯**（AGENTS.md 黑名單；圖書館推送只屬通告模組）
- ❌ 下游唔自行開成員戶口、唔反寫上游（身份錨點在該團支部）

---

## 9. 清理（版號）

- `apps-script/Code.gs` 同 `assets/batch-onboard/Code.gs` 內所有 `// vX.X` 註解全拆（連 `initializeSheets()` 彈窗嘅版號字樣）。
- **版號只留本 MD**；有守護測試（GS 再出現版號字樣即 fail）。
- `operations/`／`docs/` 嘅維運 MD 已入 `.vercelignore` 同 build 白名單之外 → 只留 Git，唔會部署。

---

## 10. 驗收

**本輪實跑（2026-09-23，兩個 repo HEAD）**

| 項目 | vsbadge | roverbadge |
|---|---|---|
| `npm run test:link`（`tests/troop_link.test.mjs`） | **10 / 10 綠** | **10 / 10 綠** |
| `npm test` | e2e **153 通過 0 失敗** | exit 0（check 46/46、lint 3/3、e2e 151、build 1/1） |
| `Code.gs` | 79,509 B / 1,305 行 → **118,805 B / 1,931 行** | 對齊版（內容維持） |
| build output | 768,392 B → **807,674 B** | **778,435 B** |
| 前端 `index.html`／`api/` | **零改動** | **零改動** |

`troop_link.test.mjs` 10 項守：① 掣未設定＝行為零變化 ② 閂口後直接登入／申請／load／apikey save 全拒 ③ 登記後 sig 可讀可寫＋上游可閂下游掣＋非 GAS URL 拒 ④ sig 防護（錯 key／竄改／過期／未來 ts／重放／混合傳送／白名單外）⑤ ABCD 唔落任何工作表 ⑥ 上游開戶＝兩邊同一 hash＋冪等＋冇帶 hash 保留原密碼 ⑦ 匯出匯入保留舊密碼＋明文密碼拒 ⑧ 批量 `importUsers`＋`getAllUsers` 唔洩漏 hash ⑨ 標籤唔會變算式 ⑩ GS 內冇版號註解。

> 本地測試唔能代替正式 GAS 部署驗收：`sig` 經真實 302 轉址、Drive 權限、Script Properties 配額、`onOpen` 選單授權，都要喺測試旅團真機跑一次先算數。

---

## 11. 其他支部升級（cubsbadge／scoutbadge）— 差異同清單

現時生態圈有**兩套 sig 家族**，呢個係「其他支部門」最大嘅阻礙：

| | **家族 B（本輪行李）** vsbadge／roverbadge | **家族 A（v4 葉）** cubsbadge／scoutbadge |
|---|---|---|
| sig 輸入 | `action + ts + nonce + sha256(body)` | `childId + sub + scope/role + children + target + exp（+jti）` |
| 密鑰推導 | `HMAC("<repo>-troop-sig-v1", D)` | 直接用單位 apikey 做 HMAC key |
| 防重放 | ±5 分鐘 + nonce 一次性 + body digest | **冇 nonce、冇 ts**：重放窗口＝TTL（30–60 分鐘） |
| 傳送 | query + body 雙通道（防 302 丟失） | body 一次 |
| 登記位置 | 上游 Script Properties `DOWNSTREAM_<id>_*` | 冇（靠 Vercel env／`setDownstreamAccess`） |
| 中央登入 | GAS 回打 `/api/super` 驗票 | **零回打**：Vercel 驗 `SUPER_KEY` → `action=superLogin` |
| 已有 action | `upsertUser`／`importUsers`／`resetPassword` | `upsertUser`／`setPw`／`verifyPw`／`exportAll(include_hash)`／`importAll`／`getRegistrySafe` |

**升級其他支部時，照下面做：**

1. 保留家族 A 已有嘅好嘢：`setPw`／`verifyPw`（BUILD.md §2 要求嘅密碼同步核對）、`exportAll({include_hash})`／`importAll`、`getRegistrySafe`（剝皮名冊）、`getDownstreamAccess`（讀掣）。
2. 換入家族 B 嘅 sig 數學（`ts + nonce + body digest` 雙通道）＋ 12 讀／19 寫白名單 ＋ 永不接受清單。
3. 加 `DOWNSTREAM_<id>_*` 登記 + `callDownstream()` + 選單（登記／測試連線／閂口／為下游開戶／匯出／匯入）。
4. **`<purpose>` 統一**做同一個字串（跨 repo 共用，見 §12 ①）——唔統一，旅系統同其他支部永遠接唔到。
5. **action 名統一**（`setUserStatus` vs `deactivateUser`／`reactivateUser`、`bulkAddUsers`、`getAuditLog`、`getLogRequests`／`getPendingLogRequests`，見 §12 ②）。
6. 每層 Sheet 選單、`initializeSheets()` 彈窗、GS 內版號註解清理（§9）＋ 加 `tests/troop_link.test.mjs` 同等 10 項守護測試。
7. `docs/AGENT_TEMPLATE_FOR_OTHER_SECTIONS.md`（roverbadge）同 cubsbadge 嘅 `docs/旅系統升級_AGENT執行版.md` 要同步更新到本 MD 為止——舊版仍然寫住「`Code.gs` 完全不用改」、「17-21 文件」等已過時內容。

---

## 12. 待決（要你批，批完先開工升級其他支部）

| # | 事項 | 建議 |
|---|---|---|
| ① | `<purpose>` 字串跨 repo 唔統一（`vsbadge-troop-sig-v1`／`roverbadge-troop-sig-v1`） | 統一用一個生態圈常數（例 `ecportal-troop-sig-v1`），各 repo 改成常數引用；未統一前，接駁只可以喺**同一個 repo** 內做 |
| ② | action 名唔一致（`setUserStatus`／`deactivateUser`／`reactivateUser`、`bulkAddUsers`、`getAuditLog`） | 定一份**最低共通 action 契約**（讀／寫各一表），各 repo 補齊別名 |
| ③ | 旅層 registry 位置：BUILD.md §1 寫 `TROOP_OPS Sheet 每團一行（支部＋進度兩把 key）＋Vercel 讀 + cache 5 分鐘`，實作係**上游 GAS Script Properties** | 兩者定位要寫死：現行以 Script Properties 為 sig 唯一來源；`TROOP_OPS` 只喺「Vercel 側真係要讀旅層」先建，唔好同時有兩份真理（BUILD.md 已加註） |
| ④ | 支部前端「進度」頁（BUILD.md §1／§4：經 `PROGRESS_BACKEND/APIKEY` 打進度 `/exec`）**全生態圈零實作**；而且**閂口後 apikey 路徑一齊失效**，前端唯一出路係「經自己上游 GAS 用 sig 轉發」 | 未起好「經上游」路徑之前，**唔好閂下游直接入口**（BUILD.md 已加註）；起好之後先按 §2 步驟 6 閂 |
| ⑤ | 中央登入兩套並存（回打 `/api/super` vs 零回打 `superLogin`） | 建議新裝／其他支部一律用**零回打**（少一個會斷嘅回打路、少一個受信端點）；已上線嘅回打版唔動（唔郁 A） |
| ⑥ | `verifyPw` 背景核對（BUILD.md §2：支部改密碼 `setPw` 同步 + `verifyPw` 核對，唔一致常駐提示）喺家族 B **未實作** | 升級其他支部時要補（家族 A 已有 `verifyPw` 可抄） |

---

## 附錄 A：原文清單（2026-09-22，照抄唔改字，只作歷史）

> 舊進度已完成，現升級旅系統。照做就得。

**變數 ABCD（唔入 SHEET）**

- A = `SUPER_KEY`（超管隱藏，與旅無關）
- B = `TROOP_(id)_BACKEND`（部署後抄 URL）
- C = `TROOP_(id)_NAME`（交 ADMIN 時填）
- D = `TROOP_(id)_APIKEY`（生成後交 ADMIN 轉登記）

B/D 由 GS 生成，C 自填，A 隱藏。**四個唔寫入 SHEET**，經收件匣交 ADMIN。

**上下游接入**

1. **部署 GS**，抄 B 的 URL，生成 D
2. **填 C**，連 B/D 經收件匣交 ADMIN
3. **每團補一張支部 SHEET**，與現有進度 SHEET 成對
4. **上游控下游寫**：掣在上游（旅控團、團控進度），旗 `ALLOW_LOCAL_LOGIN` 寫在下游 `ScriptProperties`，閂後只收 `sig`
5. **開戶**：閂口後新戶在上游揀團開戶，經 `sig` 落下游寫

**吐 JSON（搬舊數）**

- 舊進度有數，新支部空 → 在**舊進度按「匯出 JSON（含 hash）」**
- 在**新支部按「匯入」**，逐個 `upsertUser` 直插 hash（保留舊密碼）
- 匯完可閂下游直接入口

**唔做**

- 不在 SHEET 寫 ABCD
- 不改 A
- 不設回調

**清理**

- GS 內 `// vX.X` 註解全拆，版號只留本 MD
