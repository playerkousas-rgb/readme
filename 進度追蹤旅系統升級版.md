# 進度追蹤旅系統升級版 — 跟住改

> 舊進度已完成，現要升級做旅系統。跟下面做就得。

## 變數 ABCD

- A = `SUPER_KEY`（超管，隱藏）
- B = `TROOP_(id)_BACKEND`
- C = `TROOP_(id)_NAME`
- D = `TROOP_(id)_APIKEY`

B/C/D 為旅接入用，A 隱藏不顯示。`id` 為旅團代號（例 0082）。

## 要做

1. **預設走上游**  
   新功能預設經上游接入

2. **C 加一行**  
   每團一行，記 B 同 D 的連結

3. **開關**  
   掣在上游，旗寫在下游，閂後只收上游

4. **開戶**  
   閂口後在上游揀團開戶

## 唔做

- 不改 A
- 不在 SHEET 寫 B/D 的 URL/KEY
- 不設回調
